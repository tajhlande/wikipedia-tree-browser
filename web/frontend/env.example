# Environment variables for WP Embeddings Visualization

# API Base URL - Overrides the default API endpoint
# If not set, the application will try to construct the URL from the current hostname
# If that fails, it will fall back to http://localhost:8000/api
VITE_API_BASE_URL=http://localhost:8000/api
